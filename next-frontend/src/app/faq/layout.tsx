export const metadata = {
    title: 'FAQ',
    description: 'Frequently asked questions about feelinga teas — shipping, brewing, returns, and more.',
};

export default function FaqLayout({ children }: { children: React.ReactNode }) {
    return children;
}
