export const metadata = {
    title: 'Tea Gifting',
    description: 'Beautifully curated tea gift sets for every occasion. Corporate gifting, birthdays, weddings, and festivals.',
};

export default function GiftingLayout({ children }: { children: React.ReactNode }) {
    return children;
}
