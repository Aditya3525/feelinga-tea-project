export const metadata = {
    title: 'Checkout',
    description: 'Complete your order at Feelinga. Secure checkout with multiple payment options.',
};

export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
    return children;
}
