export const metadata = {
    title: 'Tea Knowledge',
    description: 'Learn about tea — brewing guides, health benefits, origins, and the art of tea appreciation.',
};

export default function LearnLayout({ children }: { children: React.ReactNode }) {
    return children;
}
