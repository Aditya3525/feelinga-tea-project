export const metadata = {
    title: 'My Account',
    description: 'Manage your feelinga account — profile, orders, addresses, and preferences.',
};

export default function ProfileLayout({ children }: { children: React.ReactNode }) {
    return children;
}
